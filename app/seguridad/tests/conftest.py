# Fixtures compartidas (client, pb, usuario_factory, rol_administrador,
# rol_agente, admin_client) viven en el conftest.py de la raíz del repo —
# pytest las descubre automáticamente, no requieren import aquí.
